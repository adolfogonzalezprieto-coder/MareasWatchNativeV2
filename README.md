# Mareas Watch Native V2
Proyecto Wear OS nativo. GPS activo, pantalla Buscando GPS, coordenadas visibles e icono propio.

La raíz correcta contiene solo `.github`, `app`, `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties` y `README.md`.
No existe una carpeta `android`, ni React, Capacitor, `package.json`, `server.ts` o `App.tsx`.
